package com.haodaone.software.entity;

public enum SoftwareDeploymentStatus {
    PENDING,
    DOWNLOADING,
    INSTALLING,
    INSTALLED,
    ALREADY_INSTALLED,
    FAILED,
    CANCELLED,
    DEVICE_OFFLINE
}
